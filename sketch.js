let table; 
let points = [];

function preload() {
    table = loadTable('freedom_with_continent.csv', 'csv', 'header');
}

function setup() {
    createCanvas(1280, 720); 
    colorMode(HSB, 360, 100, 100);
    console.log("number of continents: ");

}
function draw() {
    background (51);

}

Hallo 
